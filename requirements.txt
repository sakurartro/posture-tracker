# opencv-contrib-python is pulled in transitively by mediapipe and provides
# the full cv2 API we need; installing opencv-python separately causes a
# conflicting duplicate "cv2" package, so it's intentionally not listed here.
mediapipe==0.10.35
rich>=13.7
